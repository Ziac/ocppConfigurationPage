# Node.js Starter Project

A modern Node.js starter project using ES modules and the built-in HTTP module.

## Requirements

- Node.js >= 18.0.0

## Scripts

- `npm start`: Start the server
- `npm run dev`: Start the server with watch mode
- `npm test`: Run tests

## API

The server exposes a single endpoint that returns a JSON response with a message and timestamp.